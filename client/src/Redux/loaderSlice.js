//////// THIS DASHBOARD WILL DISPLAY USER BASED ON THEIR PROFESSION

/*
/
/ const RoleBasedHome = () => {
  const { user } = useSelector(state => state.userReducer);

  switch (user.role) {
    case 'farmer':
      return <FarmerDashboard />;
    case 'manager':
      return <ManagerDashboard />;
    case 'expert':
      return <ExpertDashboard />;
    default:
      return <div>Unknown role</div>;
  }
};
 */